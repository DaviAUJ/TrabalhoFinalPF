// Fazer as páginas que vão aparecer dps que o jogador clicar em uma das opções do menu inicial

// Fazer com que elas tbm tenha um efeito de hover

// e tentar corrigir as partes que não estão 100% funcional(se nn der é ok, só deixar o menos possível)

// Hospedar o site pra ter aquele ponto extra dos crias

// parar o contador nas funções de ganhar e perder

// Modo customizado (se der)

// botar um easter egg (se der)

// Mexer nas cores do quadrados para mais fácil vizualição(tipo tirar aquele branco e escurecer casas ja clicadsaassa)

// e fazer a fdunção de botar cor nos numeros dependo do valor ( se der )